# SPEC-08: CLI Subcommands + Config File

**Project:** `ctxlite`  
**Scope:** `ctxlite stats` CLI, `ctxlite cache` subcommand, `config.yaml` support  
**Depinde de:** SPEC-01 (main.go, stats.Store), SPEC-03 (cache)  
**Agent target:** Cursor AI  
**Status:** `[x] Done`

---

## Context

Pana acum `main.go` are un singur mod de rulare: porneste proxy + MCP server.  
Aceasta spec adauga subcommand-uri si config file:

```bash
ctxlite                    # default: porneste proxy + MCP (comportament existent)
ctxlite stats              # afiseaza statistici si iese
ctxlite stats --last 7d
ctxlite stats --export json
ctxlite cache clear        # sterge cache si iese
ctxlite cache stats
ctxlite cache clear --l2
```

---

## Arhitectura `internal/cli/`

```
internal/cli/
├── doc.go
├── cli.go          ← parse subcommand + dispatch
├── stats.go        ← implementare subcomand stats
├── cache.go        ← implementare subcomand cache
└── cli_test.go
```

---

## `cli.go` — Router principal

```go
package cli

import (
    "fmt"
    "os"

    "github.com/ctxlite/ctxlite/go/internal/cache"
    "github.com/ctxlite/ctxlite/go/internal/stats"
)

// Deps contine dependintele necesare subcommand-urilor.
type Deps struct {
    Store *stats.Store
    Cache *cache.Cache
}

// Run parseaza argumentele si dispatcha la subcomand corect.
// Returneaza exit code (0 = success, 1 = eroare).
func Run(args []string, deps Deps) int {
    if len(args) == 0 {
        // Niciun subcommand — comportament default (proxy + MCP)
        // Semnalat catre main.go prin return -1 (sentinel)
        return -1
    }

    switch args[0] {
    case "stats":
        return runStats(args[1:], deps)
    case "cache":
        return runCache(args[1:], deps)
    case "help", "--help", "-h":
        printHelp()
        return 0
    default:
        fmt.Fprintf(os.Stderr, "ctxlite: unknown subcommand %q\n", args[0])
        fmt.Fprintf(os.Stderr, "Run 'ctxlite help' for usage.\n")
        return 1
    }
}

func printHelp() {
    fmt.Fprint(os.Stderr, `ctxlite — token optimizer for Cursor, OpenCode and Claude Code

USAGE:
  ctxlite [flags]              Start proxy + MCP server
  ctxlite stats [flags]        Show token savings statistics
  ctxlite cache <command>      Manage local cache
  ctxlite help                 Show this help

PROXY FLAGS:
  --port int              HTTP proxy port (default: 8080)
  --db string             SQLite database path (default: ~/.ctxlite/cache.db)
  --config string         Config file path (default: ~/.ctxlite/config.yaml)
  --anthropic-url string  Override Anthropic API base URL
  --openai-url string     Override OpenAI API base URL
  --embedding-url string  OpenAI-compatible embeddings URL
  --embedding-key string  API key for embedding provider
  --max-context int       Max tokens for context trimming (default: 4096)
  --verbose               Enable debug logging
  --version               Print version and exit

STATS FLAGS:
  --last string           Period: session, today, 7d, 30d, all (default: session)
  --export string         Export format: text, json (default: text)

CACHE COMMANDS:
  ctxlite cache stats     Show cache size and entry counts
  ctxlite cache clear     Clear all cache entries
  ctxlite cache clear --l1  Clear only exact match cache
  ctxlite cache clear --l2  Clear only semantic cache

EXAMPLES:
  ctxlite                          Start with defaults
  ctxlite --port 9090              Start on custom port
  ctxlite stats --last 7d          Show last 7 days savings
  ctxlite stats --export json      Export stats as JSON
  ctxlite cache clear              Clear all cached responses
  ctxlite cache clear --l2         Clear only semantic cache

CONFIGURATION:
  Config file: ~/.ctxlite/config.yaml
  All flags can be set in config. CLI flags override config values.
  See 'docs/configuration.md' for full reference.
`)
}
```

---

## `stats.go` — Subcomand `ctxlite stats`

```go
package cli

import (
    "encoding/json"
    "flag"
    "fmt"
    "os"
    "time"

    "github.com/ctxlite/ctxlite/go/internal/stats"
)

// StatsExport e structura exportata in JSON.
type StatsExport struct {
    Period          string    `json:"period"`
    GeneratedAt     time.Time `json:"generated_at"`
    TotalRequests   int       `json:"total_requests"`
    CacheHits       int       `json:"cache_hits"`
    L1Hits          int       `json:"l1_hits"`
    L2Hits          int       `json:"l2_hits"`
    HitRatePct      float64   `json:"hit_rate_pct"`
    TrimmedRequests int       `json:"trimmed_requests"`
    TokensSavedCache int64    `json:"tokens_saved_cache"`
    TokensSavedTrim  int64    `json:"tokens_saved_trim"`
    TokensSavedTotal int64    `json:"tokens_saved_total"`
    CostSavedUSD    float64   `json:"cost_saved_usd"`
    AvgLatencyMs    int64     `json:"avg_latency_ms"`
}

func runStats(args []string, deps Deps) int {
    fs := flag.NewFlagSet("stats", flag.ContinueOnError)
    last   := fs.String("last", "session", "Period: session, today, 7d, 30d, all")
    export := fs.String("export", "text", "Export format: text, json")

    if err := fs.Parse(args); err != nil {
        fmt.Fprintf(os.Stderr, "ctxlite stats: %v\n", err)
        return 1
    }

    if deps.Store == nil {
        fmt.Fprintln(os.Stderr, "ctxlite stats: database not available")
        return 1
    }

    // Calculeaza intervalul
    since := parsePeriod(*last)
    summary := deps.Store.SummaryFrom(since)

    switch *export {
    case "json":
        return printStatsJSON(summary, *last)
    default:
        printStatsText(summary, *last)
        return 0
    }
}

func parsePeriod(period string) time.Time {
    switch period {
    case "session":
        // Sesiunea curenta — de la pornirea procesului
        // Pentru CLI standalone (fara proxy pornit), folosim today
        return time.Now().Truncate(24 * time.Hour)
    case "today":
        now := time.Now()
        return time.Date(now.Year(), now.Month(), now.Day(), 0, 0, 0, 0, now.Location())
    case "7d":
        return time.Now().AddDate(0, 0, -7)
    case "30d":
        return time.Now().AddDate(0, 0, -30)
    case "all":
        return time.Time{} // zero = no filter
    default:
        fmt.Fprintf(os.Stderr, "ctxlite stats: unknown period %q, using 'today'\n", period)
        return time.Now().Truncate(24 * time.Hour)
    }
}

func printStatsText(s stats.Summary, period string) {
    hitRate := 0.0
    if s.TotalRequests > 0 {
        hitRate = float64(s.CacheHits) / float64(s.TotalRequests) * 100
    }

    total := s.TokensSavedCache + s.TokensSavedTrim

    fmt.Printf(`
ctxlite stats — %s
─────────────────────────────────────────
Requests      %d total
Cache hits    %d (%.1f%%)
  L1 exact    %d
  L2 semantic %d
Trimmed       %d requests

Tokens saved
  Cache       %s
  Trimming    %s
  Total       %s

Cost saved    ~$%.4f

Avg latency   %dms (on misses)
─────────────────────────────────────────
`,
        periodLabel(period),
        s.TotalRequests,
        s.CacheHits, hitRate,
        s.L1Hits,
        s.L2Hits,
        s.TrimmedRequests,
        formatTokens(s.TokensSavedCache),
        formatTokens(s.TokensSavedTrim),
        formatTokens(total),
        s.CostSaved,
        s.AvgLatencyMs,
    )
}

func printStatsJSON(s stats.Summary, period string) int {
    total := s.TokensSavedCache + s.TokensSavedTrim
    hitRate := 0.0
    if s.TotalRequests > 0 {
        hitRate = float64(s.CacheHits) / float64(s.TotalRequests) * 100
    }

    export := StatsExport{
        Period:           periodLabel(period),
        GeneratedAt:      time.Now().UTC(),
        TotalRequests:    s.TotalRequests,
        CacheHits:        s.CacheHits,
        L1Hits:           s.L1Hits,
        L2Hits:           s.L2Hits,
        HitRatePct:       hitRate,
        TrimmedRequests:  s.TrimmedRequests,
        TokensSavedCache: s.TokensSavedCache,
        TokensSavedTrim:  s.TokensSavedTrim,
        TokensSavedTotal: total,
        CostSavedUSD:     s.CostSaved,
        AvgLatencyMs:     s.AvgLatencyMs,
    }

    enc := json.NewEncoder(os.Stdout)
    enc.SetIndent("", "  ")
    if err := enc.Encode(export); err != nil {
        fmt.Fprintf(os.Stderr, "ctxlite stats: encode error: %v\n", err)
        return 1
    }
    return 0
}

func periodLabel(period string) string {
    switch period {
    case "session": return "current session"
    case "today":   return "today"
    case "7d":      return "last 7 days"
    case "30d":     return "last 30 days"
    case "all":     return "all time"
    default:        return period
    }
}

func formatTokens(n int64) string {
    switch {
    case n >= 1_000_000:
        return fmt.Sprintf("%.1fM", float64(n)/1_000_000)
    case n >= 1_000:
        return fmt.Sprintf("%.1fK", float64(n)/1_000)
    default:
        return fmt.Sprintf("%d", n)
    }
}
```

---

## `cache.go` — Subcomand `ctxlite cache`

```go
package cli

import (
    "flag"
    "fmt"
    "os"
)

func runCache(args []string, deps Deps) int {
    if len(args) == 0 {
        fmt.Fprintln(os.Stderr, "ctxlite cache: missing command (stats, clear)")
        fmt.Fprintln(os.Stderr, "Run 'ctxlite help' for usage.")
        return 1
    }

    switch args[0] {
    case "stats":
        return runCacheStats(deps)
    case "clear":
        return runCacheClear(args[1:], deps)
    default:
        fmt.Fprintf(os.Stderr, "ctxlite cache: unknown command %q\n", args[0])
        return 1
    }
}

// runCacheStats afiseaza dimensiunea si numarul de intrari din cache.
func runCacheStats(deps Deps) int {
    if deps.Store == nil {
        fmt.Fprintln(os.Stderr, "ctxlite cache stats: database not available")
        return 1
    }

    cs := deps.Store.CacheStats()

    fmt.Printf(`
ctxlite cache stats
─────────────────────────────────
L1 (exact match)
  Entries     %d / %d max
  Size        %s
  Oldest      %s

L2 (semantic)
  Entries     %d
  Size        %s
  Enabled     %v

Total size    %s
─────────────────────────────────
`,
        cs.L1Entries, cs.L1MaxEntries,
        formatBytes(cs.L1SizeBytes),
        formatAge(cs.L1OldestEntry),
        cs.L2Entries,
        formatBytes(cs.L2SizeBytes),
        cs.L2Enabled,
        formatBytes(cs.L1SizeBytes+cs.L2SizeBytes),
    )
    return 0
}

// runCacheClear sterge intrarile din cache.
func runCacheClear(args []string, deps Deps) int {
    fs := flag.NewFlagSet("cache clear", flag.ContinueOnError)
    onlyL1 := fs.Bool("l1", false, "Clear only exact match cache (L1)")
    onlyL2 := fs.Bool("l2", false, "Clear only semantic cache (L2)")

    if err := fs.Parse(args); err != nil {
        fmt.Fprintf(os.Stderr, "ctxlite cache clear: %v\n", err)
        return 1
    }

    if deps.Store == nil {
        fmt.Fprintln(os.Stderr, "ctxlite cache clear: database not available")
        return 1
    }

    // Default: sterge tot
    clearL1 := !*onlyL2
    clearL2 := !*onlyL1

    var totalCleared int64

    if clearL1 {
        n, err := deps.Store.ClearL1()
        if err != nil {
            fmt.Fprintf(os.Stderr, "ctxlite cache clear: L1 error: %v\n", err)
            return 1
        }
        totalCleared += n
        fmt.Fprintf(os.Stderr, "L1 cache cleared: %d entries removed\n", n)
    }

    if clearL2 {
        n, err := deps.Store.ClearL2()
        if err != nil {
            fmt.Fprintf(os.Stderr, "ctxlite cache clear: L2 error: %v\n", err)
            return 1
        }
        totalCleared += n
        fmt.Fprintf(os.Stderr, "L2 cache cleared: %d entries removed\n", n)
    }

    fmt.Fprintf(os.Stderr, "Done. %d total entries removed.\n", totalCleared)
    return 0
}

func formatBytes(n int64) string {
    switch {
    case n >= 1024*1024*1024:
        return fmt.Sprintf("%.1f GB", float64(n)/(1024*1024*1024))
    case n >= 1024*1024:
        return fmt.Sprintf("%.1f MB", float64(n)/(1024*1024))
    case n >= 1024:
        return fmt.Sprintf("%.1f KB", float64(n)/1024)
    default:
        return fmt.Sprintf("%d B", n)
    }
}

func formatAge(t time.Time) string {
    if t.IsZero() {
        return "N/A"
    }
    age := time.Since(t)
    switch {
    case age < time.Hour:
        return fmt.Sprintf("%d minutes ago", int(age.Minutes()))
    case age < 24*time.Hour:
        return fmt.Sprintf("%.1f hours ago", age.Hours())
    default:
        return fmt.Sprintf("%.0f days ago", age.Hours()/24)
    }
}
```

---

## Update `stats.Store` — metode noi

Adauga in `internal/stats/stats.go`:

```go
// CacheStatsResult contine informatii despre starea cache-ului.
type CacheStatsResult struct {
    L1Entries    int64
    L1MaxEntries int
    L1SizeBytes  int64
    L1OldestEntry time.Time
    L2Entries    int64
    L2SizeBytes  int64
    L2Enabled    bool
}

// CacheStats returneaza statistici despre cache.
func (s *Store) CacheStats() CacheStatsResult {
    var res CacheStatsResult

    // L1 entries + size
    _ = s.db.QueryRow(`SELECT COUNT(*), COALESCE(SUM(LENGTH(entry)), 0) FROM cache_exact`).
        Scan(&res.L1Entries, &res.L1SizeBytes)

    // L1 oldest entry
    var oldestUnix sql.NullInt64
    _ = s.db.QueryRow(`SELECT MIN(created_at) FROM cache_exact`).Scan(&oldestUnix)
    if oldestUnix.Valid {
        res.L1OldestEntry = time.Unix(oldestUnix.Int64, 0)
    }

    res.L1MaxEntries = 10_000 // din config, hardcodat in v1

    // L2 entries + size
    _ = s.db.QueryRow(`SELECT COUNT(*) FROM cache_semantic_meta`).Scan(&res.L2Entries)
    _ = s.db.QueryRow(`SELECT COALESCE(SUM(LENGTH(entry)), 0) FROM cache_semantic_meta`).
        Scan(&res.L2SizeBytes)

    // L2 enabled — verifica daca tabela vec exista
    var tblCount int
    _ = s.db.QueryRow(`
        SELECT COUNT(*) FROM sqlite_master
        WHERE type='table' AND name='cache_semantic_vec'
    `).Scan(&tblCount)
    res.L2Enabled = tblCount > 0

    return res
}

// ClearL1 sterge toate intrarile din L1 cache.
// Returneaza numarul de intrari sterse.
func (s *Store) ClearL1() (int64, error) {
    res, err := s.db.Exec(`DELETE FROM cache_exact`)
    if err != nil {
        return 0, err
    }
    return res.RowsAffected()
}

// ClearL2 sterge toate intrarile din L2 cache.
func (s *Store) ClearL2() (int64, error) {
    // Sterge din ambele tabele L2 intr-o tranzactie
    tx, err := s.db.Begin()
    if err != nil {
        return 0, err
    }
    defer tx.Rollback()

    res, err := tx.Exec(`DELETE FROM cache_semantic_meta`)
    if err != nil {
        return 0, err
    }
    n, _ := res.RowsAffected()

    _, err = tx.Exec(`DELETE FROM cache_semantic_vec`)
    if err != nil {
        return 0, err
    }

    return n, tx.Commit()
}
```

---

## `config.yaml` — Format si Loading

### Format

```yaml
# ~/.ctxlite/config.yaml

# HTTP proxy
port: 8080

# SQLite database
db: ~/.ctxlite/cache.db

# Upstream API URLs (optional overrides)
anthropic_url: https://api.anthropic.com
openai_url: https://api.openai.com

# Embedding provider pentru L2 semantic cache (optional)
# Daca lipseste, L2 e disabled
embedding:
  url: http://localhost:11434    # ex: Ollama local
  key: ""                        # API key (daca e necesar)
  model: text-embedding-3-small

# Context trimming
trimming:
  max_context_tokens: 4096
  min_bm25_score: 0.1
  import_graph: true

# Cache
cache:
  ttl: 24h
  semantic_threshold: 0.92
  max_entries: 10000

# Logging
verbose: false
```

### `internal/config/config.go`

```go
package config

import (
    "fmt"
    "os"
    "path/filepath"
    "time"

    "gopkg.in/yaml.v3"
)

// Config contine toate setarile ctxlite.
// Valorile din CLI flags suprascriu config file.
type Config struct {
    Port         int           `yaml:"port"`
    DB           string        `yaml:"db"`
    AnthropicURL string        `yaml:"anthropic_url"`
    OpenAIURL    string        `yaml:"openai_url"`
    Embedding    EmbeddingConf `yaml:"embedding"`
    Trimming     TrimmingConf  `yaml:"trimming"`
    Cache        CacheConf     `yaml:"cache"`
    Verbose      bool          `yaml:"verbose"`
}

type EmbeddingConf struct {
    URL   string `yaml:"url"`
    Key   string `yaml:"key"`
    Model string `yaml:"model"`
}

type TrimmingConf struct {
    MaxContextTokens int     `yaml:"max_context_tokens"`
    MinBM25Score     float64 `yaml:"min_bm25_score"`
    ImportGraph      bool    `yaml:"import_graph"`
}

type CacheConf struct {
    TTL               time.Duration `yaml:"ttl"`
    SemanticThreshold float64       `yaml:"semantic_threshold"`
    MaxEntries        int           `yaml:"max_entries"`
}

// Defaults returneaza configuratia default.
func Defaults() Config {
    home, _ := os.UserHomeDir()
    return Config{
        Port:         8080,
        DB:           filepath.Join(home, ".ctxlite", "cache.db"),
        AnthropicURL: "https://api.anthropic.com",
        OpenAIURL:    "https://api.openai.com",
        Embedding: EmbeddingConf{
            Model: "text-embedding-3-small",
        },
        Trimming: TrimmingConf{
            MaxContextTokens: 4096,
            MinBM25Score:     0.1,
            ImportGraph:      true,
        },
        Cache: CacheConf{
            TTL:               24 * time.Hour,
            SemanticThreshold: 0.92,
            MaxEntries:        10_000,
        },
    }
}

// DefaultPath returneaza calea default a config file-ului.
func DefaultPath() string {
    home, _ := os.UserHomeDir()
    return filepath.Join(home, ".ctxlite", "config.yaml")
}

// Load citeste config file-ul si il merge cu defaults.
// Daca fisierul nu exista, returneaza defaults (nu e eroare).
func Load(path string) (Config, error) {
    cfg := Defaults()

    // Expandeaza ~ in path
    if len(path) >= 2 && path[:2] == "~/" {
        home, _ := os.UserHomeDir()
        path = filepath.Join(home, path[2:])
    }

    data, err := os.ReadFile(path)
    if os.IsNotExist(err) {
        return cfg, nil // config lipsa = ok, folosim defaults
    }
    if err != nil {
        return cfg, fmt.Errorf("config: read %s: %w", path, err)
    }

    if err := yaml.Unmarshal(data, &cfg); err != nil {
        return cfg, fmt.Errorf("config: parse %s: %w", path, err)
    }

    return cfg, nil
}

// MergeFlags aplica CLI flags peste config, daca au fost setate explicit.
// flags e o map cu numele flag-ului si valoarea sa string.
// Apelat dupa flag.Parse() cu lista de flags care au fost setati explicit.
func (c *Config) MergeFlags(set map[string]string) {
    if v, ok := set["port"]; ok {
        fmt.Sscan(v, &c.Port)
    }
    if v, ok := set["db"]; ok {
        c.DB = v
    }
    if v, ok := set["anthropic-url"]; ok {
        c.AnthropicURL = v
    }
    if v, ok := set["openai-url"]; ok {
        c.OpenAIURL = v
    }
    if v, ok := set["embedding-url"]; ok {
        c.Embedding.URL = v
    }
    if v, ok := set["embedding-key"]; ok {
        c.Embedding.Key = v
    }
    if v, ok := set["embedding-model"]; ok {
        c.Embedding.Model = v
    }
    if v, ok := set["max-context"]; ok {
        fmt.Sscan(v, &c.Trimming.MaxContextTokens)
    }
    if _, ok := set["verbose"]; ok {
        c.Verbose = true
    }
}
```

**Dependinta de adaugat in `go.mod`:**
```
require (
    gopkg.in/yaml.v3 v3.0.1
)
```

---

## Update `main.go` — integrate CLI + config

```go
func main() {
    // ── 1. Flags ──────────────────────────────────────────────
    configPath := flag.String("config", config.DefaultPath(), "Config file path")
    port       := flag.Int("port", 0, "HTTP proxy port")               // 0 = use config
    dbPath     := flag.String("db", "", "SQLite database path")
    verbose    := flag.Bool("verbose", false, "Verbose logging")
    ver        := flag.Bool("version", false, "Print version")
    // ... restul flags-urilor existente

    flag.Parse()

    if *ver {
        fmt.Fprintf(os.Stderr, "ctxlite v%s\n", version)
        os.Exit(0)
    }

    // ── 2. Detecteaza subcomand ───────────────────────────────
    // Argumentele non-flag (dupa --)
    subcmdArgs := flag.Args()

    // ── 3. Incarca config ─────────────────────────────────────
    cfg, err := config.Load(*configPath)
    if err != nil {
        slog.Warn("config load failed, using defaults", "err", err)
    }

    // Aplica CLI flags peste config (prioritate CLI > config > defaults)
    explicitFlags := collectExplicitFlags()
    cfg.MergeFlags(explicitFlags)

    // Aplica flags individuale daca au valori non-zero
    if *port != 0 { cfg.Port = *port }
    if *dbPath != "" { cfg.DB = *dbPath }
    if *verbose { cfg.Verbose = true }

    // ── 4. Setup logging ──────────────────────────────────────
    level := slog.LevelInfo
    if cfg.Verbose { level = slog.LevelDebug }
    slog.SetDefault(slog.New(slog.NewTextHandler(os.Stderr, &slog.HandlerOptions{Level: level})))

    // ── 5. Deschide DB (necesara si pentru subcommand-uri) ────
    store, err := stats.New(cfg.DB)
    if err != nil {
        slog.Error("failed to open stats db", "err", err)
        os.Exit(1)
    }
    defer store.Close()

    // ── 6. Ruleaza subcomand daca e prezent ───────────────────
    if len(subcmdArgs) > 0 {
        // Pentru subcommand-uri, cache-ul nu trebuie pornit complet
        // — doar Store-ul e suficient
        exitCode := cli.Run(subcmdArgs, cli.Deps{
            Store: store,
            // Cache nu e initializat pentru subcommand-uri (nu e nevoie)
        })
        if exitCode >= 0 {
            os.Exit(exitCode)
        }
        // exitCode == -1 inseamna "nu e subcomand, continua cu proxy"
    }

    // ── 7. Porneste proxy + MCP (comportament default) ────────
    // ... codul existent din SPEC-01/02/03/04/05
}

// collectExplicitFlags returneaza flag-urile setate explicit de user
// (nu valorile default).
func collectExplicitFlags() map[string]string {
    explicit := make(map[string]string)
    flag.Visit(func(f *flag.Flag) {
        explicit[f.Name] = f.Value.String()
    })
    return explicit
}
```

---

## `cli_test.go`

### Test 1: stats — period parsing

```go
func TestParsePeriod(t *testing.T) {
    cases := []struct {
        input   string
        wantZero bool
    }{
        {"all",     true},
        {"session", false},
        {"today",   false},
        {"7d",      false},
        {"30d",     false},
    }
    for _, tc := range cases {
        t.Run(tc.input, func(t *testing.T) {
            got := parsePeriod(tc.input)
            isZero := got.IsZero()
            if isZero != tc.wantZero {
                t.Errorf("parsePeriod(%q).IsZero() = %v, want %v", tc.input, isZero, tc.wantZero)
            }
        })
    }
}
```

### Test 2: stats JSON export e valid JSON

```go
func TestRunStats_JSONExport(t *testing.T) {
    store, _ := stats.New(":memory:")
    defer store.Close()

    // Capture stdout
    old := os.Stdout
    r, w, _ := os.Pipe()
    os.Stdout = w

    code := runStats([]string{"--export", "json", "--last", "all"}, Deps{Store: store})

    w.Close()
    os.Stdout = old

    var buf bytes.Buffer
    buf.ReadFrom(r)
    output := buf.String()

    if code != 0 {
        t.Errorf("expected exit code 0, got %d", code)
    }

    var export StatsExport
    if err := json.Unmarshal([]byte(output), &export); err != nil {
        t.Errorf("invalid JSON output: %v\noutput: %s", err, output)
    }
    if export.GeneratedAt.IsZero() {
        t.Error("generated_at should not be zero")
    }
}
```

### Test 3: cache clear L1

```go
func TestRunCacheClear_L1Only(t *testing.T) {
    store := setupStoreWithEntries(t)

    // Clear doar L1
    code := runCache([]string{"clear", "--l1"}, Deps{Store: store})
    if code != 0 {
        t.Errorf("expected exit code 0, got %d", code)
    }

    cs := store.CacheStats()
    if cs.L1Entries != 0 {
        t.Errorf("L1 should be empty after clear, got %d entries", cs.L1Entries)
    }
    if cs.L2Entries == 0 {
        t.Error("L2 should not be cleared when --l1 flag is used")
    }
}
```

### Test 4: cache clear L2

```go
func TestRunCacheClear_L2Only(t *testing.T) {
    store := setupStoreWithEntries(t)

    code := runCache([]string{"clear", "--l2"}, Deps{Store: store})
    if code != 0 { t.Errorf("expected 0, got %d", code) }

    cs := store.CacheStats()
    if cs.L2Entries != 0 {
        t.Errorf("L2 should be empty, got %d entries", cs.L2Entries)
    }
    if cs.L1Entries == 0 {
        t.Error("L1 should not be cleared when --l2 flag is used")
    }
}
```

### Test 5: CLI router — subcomand necunoscut returneaza exit code 1

```go
func TestCLIRouter_UnknownSubcommand(t *testing.T) {
    code := Run([]string{"foo"}, Deps{})
    if code != 1 {
        t.Errorf("expected exit code 1 for unknown subcommand, got %d", code)
    }
}
```

### Test 6: CLI router — niciun subcomand returneaza -1 (sentinel)

```go
func TestCLIRouter_NoSubcommand_ReturnsSentinel(t *testing.T) {
    code := Run([]string{}, Deps{})
    if code != -1 {
        t.Errorf("expected sentinel -1 for no subcommand, got %d", code)
    }
}
```

### Test 7: config Load — fisier lipsa returneaza defaults

```go
func TestConfigLoad_MissingFile_ReturnsDefaults(t *testing.T) {
    cfg, err := config.Load("/tmp/ctxlite-nonexistent-config-xyz.yaml")
    if err != nil {
        t.Errorf("expected no error for missing config, got: %v", err)
    }
    if cfg.Port != 8080 {
        t.Errorf("expected default port 8080, got %d", cfg.Port)
    }
}
```

### Test 8: config Load — valori din fisier suprascriu defaults

```go
func TestConfigLoad_ValuesOverrideDefaults(t *testing.T) {
    content := []byte("port: 9090\nverbose: true\n")
    f, _ := os.CreateTemp("", "ctxlite-config-*.yaml")
    defer os.Remove(f.Name())
    f.Write(content)
    f.Close()

    cfg, err := config.Load(f.Name())
    if err != nil { t.Fatal(err) }

    if cfg.Port != 9090 { t.Errorf("expected port 9090, got %d", cfg.Port) }
    if !cfg.Verbose { t.Error("expected verbose=true") }
    if cfg.DB == "" { t.Error("DB should have default value") }
}
```

### Test 9: config MergeFlags — CLI flag suprascrie config

```go
func TestConfigMergeFlags_CLIOverridesConfig(t *testing.T) {
    cfg := config.Defaults()
    cfg.Port = 9090 // din config file

    // CLI flag --port 7777 suprascrie
    cfg.MergeFlags(map[string]string{"port": "7777"})

    if cfg.Port != 7777 {
        t.Errorf("expected port 7777 after flag merge, got %d", cfg.Port)
    }
}
```

### Helper

```go
func setupStoreWithEntries(t *testing.T) *stats.Store {
    t.Helper()
    store, _ := stats.New(":memory:")
    t.Cleanup(func() { store.Close() })

    // Insereaza cateva intrari fake in L1 si L2
    store.DB().Exec(`INSERT INTO cache_exact (key, entry, created_at, expires_at)
        VALUES ('k1', '{}', unixepoch(), unixepoch() + 86400)`)
    // L2 insert simplu (fara vector — doar metadata)
    store.DB().Exec(`INSERT INTO cache_semantic_meta (entry, text_hash, created_at, expires_at)
        VALUES ('{}', 'h1', unixepoch(), unixepoch() + 86400)`)

    return store
}
```

---

## Update `docs/configuration.md`

Adauga sectiune pentru config file:

```markdown
## Config file

Default location: `~/.ctxlite/config.yaml`

Override path:
```bash
ctxlite --config /path/to/config.yaml
```

**Priority order** (highest wins):
1. CLI flags
2. Config file
3. Built-in defaults

**Full example** — `~/.ctxlite/config.yaml`:
```yaml
port: 8080
db: ~/.ctxlite/cache.db
verbose: false

embedding:
  url: http://localhost:11434
  model: nomic-embed-text

trimming:
  max_context_tokens: 8192
  import_graph: true

cache:
  ttl: 48h
  semantic_threshold: 0.90
  max_entries: 20000
```

Config file inexistent nu e eroare — ctxlite porneste cu defaults.
```

---

## Acceptance criteria

Agentul a terminat SPEC-08 cand:

- [ ] `make build && ./bin/ctxlite help` afiseaza help-ul complet
- [ ] `./bin/ctxlite stats` afiseaza tabelul text cu zeros pentru DB goala
- [ ] `./bin/ctxlite stats --last 7d` nu crapa
- [ ] `./bin/ctxlite stats --export json | python3 -m json.tool` e valid JSON
- [ ] `./bin/ctxlite cache stats` afiseaza info despre cache
- [ ] `./bin/ctxlite cache clear` ruleaza fara eroare pe DB goala
- [ ] `./bin/ctxlite cache clear --l1` sterge doar L1, L2 ramane intact
- [ ] `./bin/ctxlite cache clear --l2` sterge doar L2, L1 ramane intact
- [ ] `./bin/ctxlite` (fara subcomand) porneste proxy + MCP ca inainte
- [ ] Config file lipsa → ctxlite porneste cu defaults, fara eroare
- [ ] CLI flag `--port 9090` suprascrie `port: 8080` din config
- [ ] `make test` — toate cele 9 teste noi trec
- [ ] `docs/configuration.md` are sectiunea config file