# SPEC-013: @ctxlite/mcp — MCP Server pentru Cursor

**Package:** `@ctxlite/mcp`  
**Scope:** MCP stdio server cu tools `trim_context` si `get_stats`, compatibil cu Cursor editor si Cursor agent CLI  
**Depinde de:** SPEC-010 (monorepo), SPEC-011 (@ctxlite/core)  
**Agent target:** Cursor  
**Status:** `[x] Done`

---

## Context

Cursor agent CLI detecteaza automat `mcp.json` si suporta aceleasi MCP servers ca editorul.
Deci un singur package acopera ambele.

**REGULA CRITICA stdio:** Niciodata `console.log()` in MCP server stdio — scrie pe stdout si corupe JSON-RPC protocol. Foloseste exclusiv `console.error()` care scrie pe stderr.

---

## Structura `packages/mcp/src/`

```
packages/mcp/src/
├── index.ts          ← entry point + shebang
├── server.ts         ← McpServer setup + tool registration
├── tools/
│   ├── trim-context.ts
│   └── get-stats.ts
└── shared.ts         ← db path shared cu opencode plugin
```

---

## `shared.ts`

```ts
import { join } from "path"
import { homedir } from "os"

/**
 * Calea catre SQLite database.
 * TREBUIE sa fie identica cu cea din @ctxlite/opencode si ctxlite CLI.
 */
export const STATS_DB_PATH = join(homedir(), ".ctxlite", "stats.db")
```

---

## `tools/get-stats.ts`

```ts
import { z } from "zod"
import { StatsStore } from "@ctxlite/core"
import { STATS_DB_PATH } from "../shared.js"

export const getStatsSchema = z.object({
  period: z
    .enum(["session", "today", "7d", "30d", "all"])
    .optional()
    .describe("Time period. Default: today"),
})

export async function handleGetStats(
  args: z.infer<typeof getStatsSchema>,
): Promise<string> {
  const period = args.period ?? "today"
  let store: StatsStore | null = null

  try {
    store = new StatsStore(STATS_DB_PATH)
    const since = periodToTimestamp(period)
    const summary = store.summary(since)

    if (summary.totalRequests === 0) {
      return `## ctxlite stats — ${period}\n\nNo requests recorded yet.`
    }

    const formatTokens = (n: number): string => {
      if (n >= 1_000_000) return `${(n / 1_000_000).toFixed(1)}M`
      if (n >= 1_000) return `${(n / 1_000).toFixed(1)}K`
      return `${n}`
    }

    return [
      `## ctxlite stats — ${period}`,
      ``,
      `| Metric | Value |`,
      `|--------|-------|`,
      `| Total requests | ${summary.totalRequests} |`,
      `| Trimmed requests | ${summary.trimmedRequests} |`,
      `| Tokens saved | ${formatTokens(summary.tokensSaved)} |`,
      `| Est. cost saved | $${summary.costSaved.toFixed(4)} |`,
      `| Avg latency | ${summary.avgLatencyMs}ms |`,
    ].join("\n")
  } catch (err) {
    return `ctxlite stats unavailable: ${err instanceof Error ? err.message : String(err)}`
  } finally {
    store?.close()
  }
}

function periodToTimestamp(period: string): number {
  const now = Math.floor(Date.now() / 1000)
  switch (period) {
    case "session":
      return now - 3600 // ultima ora
    case "today": {
      const d = new Date()
      d.setHours(0, 0, 0, 0)
      return Math.floor(d.getTime() / 1000)
    }
    case "7d":
      return now - 7 * 86400
    case "30d":
      return now - 30 * 86400
    case "all":
      return 0
    default:
      return now - 86400
  }
}
```

---

## `tools/trim-context.ts`

```ts
import { z } from "zod"
import { trimFiles, estimateTokens } from "@ctxlite/core"
import type { CodeFile } from "@ctxlite/core"

export const trimContextSchema = z.object({
  files: z
    .array(
      z.object({
        path: z.string().describe("Relative file path (e.g. src/auth/login.ts)"),
        content: z.string().describe("Full file content"),
        language: z
          .string()
          .optional()
          .describe("Programming language (e.g. typescript, go, python)"),
      }),
    )
    .min(1)
    .describe("Files to analyze for relevance"),
  query: z
    .string()
    .min(1)
    .describe("Description of your current task — used to score file relevance"),
  maxTokens: z
    .number()
    .int()
    .positive()
    .optional()
    .describe("Max token budget for selected files. Default: 4096"),
})

export async function handleTrimContext(
  args: z.infer<typeof trimContextSchema>,
): Promise<string> {
  const { files, query, maxTokens = 4096 } = args

  const codeFiles: CodeFile[] = files.map((f) => ({
    path: f.path,
    content: f.content,
    language: f.language ?? f.path.split(".").pop() ?? "",
    tokens: estimateTokens(f.content),
  }))

  const result = trimFiles(codeFiles, query, { maxTokens })

  // Nicio reducere semnificativa
  if (result.tokensSaved === 0) {
    return [
      `## trim_context`,
      ``,
      `All ${files.length} files are relevant for this task — no trimming applied.`,
      `Total tokens: ${result.tokensIn}`,
    ].join("\n")
  }

  const selectedPaths = result.files
    .map((f) => `- \`${f.path}\` (${f.tokens} tokens)`)
    .join("\n")

  const selectedSet = new Set(result.files.map((f) => f.path))
  const excludedPaths = codeFiles
    .filter((f) => !selectedSet.has(f.path))
    .map((f) => `- \`${f.path}\` (${f.tokens} tokens)`)
    .join("\n")

  return [
    `## trim_context result`,
    ``,
    `**Selected** — ${result.filesOut} of ${result.filesIn} files, ${result.tokensOut} tokens:`,
    selectedPaths,
    ``,
    `**Excluded** — saved ${result.tokensSaved} tokens (-${Math.round(result.trimRatio * 100)}%):`,
    excludedPaths,
    ``,
    `_Include only the selected files in your next prompt to reduce token usage._`,
  ].join("\n")
}
```

---

## `server.ts`

```ts
import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"
import { getStatsSchema, handleGetStats } from "./tools/get-stats.js"
import { trimContextSchema, handleTrimContext } from "./tools/trim-context.js"

const VERSION = "0.1.0"

/**
 * Creeaza si configureaza McpServer cu toate tool-urile ctxlite.
 */
export function createServer(): McpServer {
  const server = new McpServer({
    name: "ctxlite",
    version: VERSION,
    instructions: `ctxlite helps reduce token usage in your coding sessions.

Available tools:
- trim_context: Analyze files and return only the most relevant ones for your task
- get_stats: View token savings statistics

Usage tips:
- Before a large refactoring task, call trim_context with all candidate files
- Call get_stats anytime to see how much you've saved`,
  })

  // ── Tool: get_stats ──────────────────────────────────────────────────────
  server.registerTool(
    "get_stats",
    {
      title: "Get token savings stats",
      description: `Returns ctxlite token savings statistics.
Call this when the user asks about token usage, savings, costs, or ctxlite performance.
Supports periods: session (last hour), today, 7d, 30d, all.`,
      inputSchema: getStatsSchema,
    },
    async (args) => {
      const text = await handleGetStats(args)
      return { content: [{ type: "text" as const, text }] }
    },
  )

  // ── Tool: trim_context ───────────────────────────────────────────────────
  server.registerTool(
    "trim_context",
    {
      title: "Trim files to relevant ones",
      description: `Analyzes a list of files and returns only the most relevant ones for your current task.
Uses BM25 scoring and import graph analysis.
Use this before large prompts to reduce token usage significantly.
Provide the files you're considering and a description of what you're trying to do.`,
      inputSchema: trimContextSchema,
    },
    async (args) => {
      const text = await handleTrimContext(args)
      return { content: [{ type: "text" as const, text }] }
    },
  )

  return server
}
```

---

## `index.ts` — Entry point

```ts
#!/usr/bin/env node
// CRITICAL: Nu folosi console.log() — stdout e rezervat pentru MCP protocol
// Foloseste console.error() pentru logging — scrie pe stderr

import { StdioServerTransport } from "@modelcontextprotocol/sdk/server/stdio.js"
import { createServer } from "./server.js"

async function main(): Promise<void> {
  const server = createServer()
  const transport = new StdioServerTransport()

  await server.connect(transport)

  console.error("[ctxlite] MCP server running on stdio")
}

main().catch((err: unknown) => {
  console.error("[ctxlite] Fatal error:", err)
  process.exit(1)
})
```

---

## `packages/mcp/package.json` — update complet

```json
{
  "name": "@ctxlite/mcp",
  "version": "0.1.0",
  "description": "ctxlite MCP server for Cursor editor and Cursor agent CLI",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "ctxlite-mcp": "./dist/index.js"
  },
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": ["dist/"],
  "scripts": {
    "build": "tsc --build && chmod +x dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@ctxlite/core": "0.1.0",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
```

> **Nota:** `chmod +x dist/index.js` dupa build e necesar pe Unix pentru ca fisierul sa fie executabil direct via `npx`.

---

## Configurare in Cursor

### Cursor editor (GUI)

Creeaza sau editeaza `~/.cursor/mcp.json`:

```json
{
  "mcpServers": {
    "ctxlite": {
      "command": "npx",
      "args": ["-y", "@ctxlite/mcp"]
    }
  }
}
```

### Cursor agent CLI

Exact aceeasi configurare — agent CLI detecteaza automat si respecta `mcp.json`.

```bash
# Verifica ca serverul e detectat
agent mcp list

# Ar trebui sa apara:
# ctxlite (connected)

# Verifica tools
agent mcp list-tools ctxlite
```

### Per-proiect (shared cu echipa)

```json
// .cursor/mcp.json (in repo, commit-uit)
{
  "mcpServers": {
    "ctxlite": {
      "command": "npx",
      "args": ["-y", "@ctxlite/mcp"]
    }
  }
}
```

---

## Cum foloseste Cursor tools-urile

**get_stats — automat cand userul intreaba:**
```
User: how many tokens has ctxlite saved today?
Cursor: [calls get_stats({ period: "today" })]
        ## ctxlite stats — today
        | Metric | Value |
        |--------|-------|
        | Total requests | 23 |
        | Trimmed requests | 8 |
        | Tokens saved | 12.4K |
        | Est. cost saved | $0.0372 |
        | Avg latency | 0ms |
```

**trim_context — la cerere inainte de task mare:**
```
User: I want to refactor the auth module, help me trim the context first
Cursor: [calls trim_context({ files: [...], query: "refactor auth module" })]
        ## trim_context result
        Selected — 3 of 11 files, 1,240 tokens:
        - `src/auth/login.ts` (420 tokens)
        - `src/auth/session.ts` (380 tokens)
        - `src/types/user.ts` (440 tokens)

        Excluded — saved 8,760 tokens (-87%):
        - `src/render/template.ts` (1,200 tokens)
        ...
```

---

## `packages/mcp/src/tools/get-stats.test.ts`

```ts
import { describe, it, expect } from "vitest"
import { handleGetStats } from "./get-stats.js"

describe("handleGetStats", () => {
  it("returns formatted stats for empty db", async () => {
    // Cu db non-existent, trebuie sa returneze mesaj clar, nu sa crape
    const result = await handleGetStats({ period: "today" })
    // Fie "No requests", fie eroare formatata — niciodata throw
    expect(typeof result).toBe("string")
    expect(result.length).toBeGreaterThan(0)
  })

  it("accepts all valid periods without throwing", async () => {
    const periods = ["session", "today", "7d", "30d", "all"] as const
    for (const period of periods) {
      const result = await handleGetStats({ period })
      expect(typeof result).toBe("string")
    }
  })

  it("uses today as default when period is undefined", async () => {
    const result = await handleGetStats({})
    expect(result).toContain("today")
  })
})
```

## `packages/mcp/src/tools/trim-context.test.ts`

```ts
import { describe, it, expect } from "vitest"
import { handleTrimContext } from "./trim-context.js"

describe("handleTrimContext", () => {
  const authFile = {
    path: "src/auth/login.ts",
    content: `export function login(user: string, pass: string) {
      return jwt.sign({ user }, process.env.JWT_SECRET)
    }`,
    language: "typescript",
  }

  const templateFile = {
    path: "src/render/template.ts",
    content: `export function renderHTML(name: string) {
      return templates[name]
    }`,
    language: "typescript",
  }

  const migrationFile = {
    path: "src/db/migrate.ts",
    content: `export function runMigrations(db: Database) {
      return schema.up(db)
    }`,
    language: "typescript",
  }

  it("selects auth file for login query", async () => {
    const result = await handleTrimContext({
      files: [authFile, templateFile, migrationFile],
      query: "implement user login with JWT authentication",
    })

    expect(result).toContain("src/auth/login.ts")
  })

  it("returns all files message when no trimming needed", async () => {
    // Un singur fisier nu poate fi trimmat
    const result = await handleTrimContext({
      files: [authFile],
      query: "login",
    })

    expect(result).toContain("All")
    expect(result).toContain("no trimming applied")
  })

  it("respects maxTokens budget", async () => {
    const bigFile = {
      path: "big.ts",
      content: "x".repeat(20000), // ~5000 tokens
      language: "typescript",
    }
    const smallFile = {
      path: "small.ts",
      content: "export function tiny() { return true }",
      language: "typescript",
    }

    const result = await handleTrimContext({
      files: [bigFile, smallFile],
      query: "tiny function",
      maxTokens: 50,
    })

    // small.ts trebuie selectat, big.ts exclus
    expect(result).toContain("small.ts")
    expect(result).toContain("big.ts")
  })

  it("returns string, never throws", async () => {
    // Edge case: fisiere goale
    await expect(
      handleTrimContext({
        files: [{ path: "empty.ts", content: "", language: "typescript" }],
        query: "something",
      }),
    ).resolves.toBeTypeOf("string")
  })
})
```

## `packages/mcp/src/server.test.ts`

```ts
import { describe, it, expect } from "vitest"
import { createServer } from "./server.js"

describe("createServer", () => {
  it("creates server without throwing", () => {
    expect(() => createServer()).not.toThrow()
  })

  it("returns McpServer instance", () => {
    const server = createServer()
    expect(server).toBeDefined()
    expect(typeof server.connect).toBe("function")
  })
})
```

---

## Acceptance criteria

- [ ] `npm run build` in `packages/mcp` compileaza fara erori
- [ ] `npm run typecheck` — zero erori TypeScript
- [ ] `npm test` — toate cele 3 suite de teste trec
- [ ] `dist/index.js` are shebang `#!/usr/bin/env node`
- [ ] `dist/index.js` e executabil (`chmod +x` aplicat de build script)
- [ ] `node packages/mcp/dist/index.js` porneste fara erori pe stderr
- [ ] **Zero `console.log()`** in tot package-ul (grep verifica)
- [ ] Adaugat in `~/.cursor/mcp.json` → `agent mcp list` afiseaza `ctxlite`
- [ ] `agent mcp list-tools ctxlite` afiseaza `trim_context` si `get_stats`
- [ ] `get_stats` returneaza string valid pentru db goala (nu crapa)
- [ ] `trim_context` cu 3 fisiere si query despre "login" → auth file selectat

---

## Verificare manuala rapida

```bash
# 1. Build
cd packages/mcp && npm run build

# 2. Test direct via stdio (simuleaza Cursor)
echo '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2024-11-05","clientInfo":{"name":"test","version":"1.0"},"capabilities":{}}}' \
  | node dist/index.js

# Trebuie sa apara pe stdout:
# {"jsonrpc":"2.0","id":1,"result":{"protocolVersion":"...","serverInfo":{"name":"ctxlite",...},...}}

# 3. List tools
echo '{"jsonrpc":"2.0","id":2,"method":"tools/list","params":{}}' \
  | node dist/index.js

# Trebuie sa apara trim_context si get_stats in lista
```

---

## Ce NU face aceasta spec

- Nu implementeaza CLI stats (→ SPEC-014)
- Nu configureaza CI/release (→ SPEC-015)
- Nu suporta Streamable HTTP transport (v2 — pentru team shared servers)
- Nu face logging in SQLite din MCP server
  (stats vin din @ctxlite/opencode plugin sau din CLI direct)